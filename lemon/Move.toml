[package]
name = "Lemon"
version = "0.0.1"


[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "testnet" }
Monolith = { local = "../monolith" }
Juice = { local = "../juice" }

[dev-dependencies]
TestHelpers = { local = "../test_helpers" }

[addresses]
lemon = "0x0"